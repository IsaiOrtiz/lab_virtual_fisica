import 'package:flutter/material.dart';

class ElectroSimulador extends StatelessWidget {
  const ElectroSimulador({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Simulación de Electricidad"));
  }
}