import 'package:flutter/material.dart';

class OpticaSim extends StatelessWidget {
  const OpticaSim({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Simulación de Óptica"));
  }
}