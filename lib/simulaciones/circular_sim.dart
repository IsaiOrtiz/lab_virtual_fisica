import 'package:flutter/material.dart';

class CircularSim extends StatelessWidget {
  const CircularSim({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Simulación de Movimiento Circular"));
  }
}