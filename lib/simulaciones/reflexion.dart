import 'package:flutter/material.dart';

class ModernaSim extends StatelessWidget {
  const ModernaSim({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Simulación de Física Moderna"));
  }
}