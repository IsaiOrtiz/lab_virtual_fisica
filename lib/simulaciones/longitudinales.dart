import 'package:flutter/material.dart';

class EnergiaSim extends StatelessWidget {
  const EnergiaSim({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Simulación de Energía"));
  }
}